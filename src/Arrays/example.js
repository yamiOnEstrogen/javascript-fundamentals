let example = [];


example.push({
    name: 'Array.from()',
    description: 'Creates a new Array instance from an array-like or iterable object.',
    example: `const arrayLike = {0: 'a', 1: 'b', 2: 'c', length: 3};`,
    options: {
        method: 'Array.from(arrayLike);',
    }
});

console.log(example);


// Path: src\Arrays\example.js